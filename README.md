Make sure you have XAMPP because I didn't deploy it to docker because of some system problem
Create DB table in MYSQL

Clone this repository

Migrate the tables
php artisan migrate

Run the backend
php artisan serve
